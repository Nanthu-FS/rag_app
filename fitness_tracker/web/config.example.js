// Copy this file to `config.js` and fill in your Supabase credentials.
// Leave them blank to run on localStorage (the app works fully offline either way).
//
//   SUPABASE_URL      -> Project Settings > API > Project URL
//   SUPABASE_ANON_KEY -> Project Settings > API > anon public key (safe for the browser)
//
// Then run schema.sql (in the project root) in the Supabase SQL editor.

window.VITAL_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
